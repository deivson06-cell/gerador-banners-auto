name: Auto Texto Jogos

on:
  workflow_dispatch:      # 👈 importante que esteja nesse nível
  schedule:
    - cron: "10 12 * * *"  # executa às 09:10 (horário de Brasília)

jobs:
  run:
    runs-on: ubuntu-latest

    steps:
      - name: 📂 Checkout do repositório
        uses: actions/checkout@v4

      - name: 🧩 Instalar Google Chrome
        run: |
          sudo apt update
          sudo apt install -y google-chrome-stable chromium-driver

      - name: 🐍 Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: 📦 Instalar dependências
        run: pip install selenium webdriver-manager requests

      - name: ⚙️ Executar automação do texto
        run: python guia.py
