name: Auto NBA Banners

on:
  schedule:
    - cron: '40 13 * * *'  # Executa às 10:40 horário de Brasília (13:40 UTC)
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest

    steps:
      - name: 📂 Checkout do repositório
        uses: actions/checkout@v4

      - name: 🧩 Instalar Google Chrome e dependências
        run: |
          sudo apt update
          sudo apt install -y google-chrome-stable chromium-driver

      - name: 🐍 Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: 📦 Instalar dependências
        run: |
          pip install selenium webdriver-manager

      - name: ⚙️ Executar automação NBA
        env:
          LOGIN: ${{ secrets.LOGIN }}
          SENHA: ${{ secrets.SENHA }}
        run: |
          echo "🏀 Iniciando fluxo NBA..."
          python nba.py
